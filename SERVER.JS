const port = process.env.PORT || 3000;const server = http.createServer((req, res) => { res.writeHead(200, {“Content-Type”: “text/plain”}); res.end(“AY Motorway API is running”); }); server.listen(port);
